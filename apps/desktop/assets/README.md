# Assets

electron-builder resources (spec §45). Place platform icons here:

- `icon.icns` — macOS
- `icon.ico` — Windows
- `icon.png` (512×512) — Linux

When absent, electron-builder falls back to the default Electron icon so
development and CI builds still succeed.
